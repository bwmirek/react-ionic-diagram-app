import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'react-ionic-diagram-app',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
